<?xml version="1.0" encoding="UTF-8"?>
<!--suppress ALL -->
<beans xmlns="http://www.springframework.org/schema/beans"
	   xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	   xmlns:tx="http://www.springframework.org/schema/tx"
	   xmlns:aop="http://www.springframework.org/schema/aop"
	   xsi:schemaLocation="http://www.springframework.org/schema/beans
		http://www.springframework.org/schema/beans/spring-beans-4.3.xsd
		 http://www.springframework.org/schema/tx 
		 http://www.springframework.org/schema/tx/spring-tx.xsd
		 http://www.springframework.org/schema/aop http://www.springframework.org/schema/aop/spring-aop.xsd">
	<!-- spring和MyBatis完美整合，不需要mybatis的配置映射文件 -->
	<bean id="sqlSessionFactory" class="org.mybatis.spring.SqlSessionFactoryBean">
		<property name="dataSource" ref="DataSource" />
		<!-- 自动扫描mapping.xml文件 -->
		<property name="mapperLocations" value="classpath:mapper/mybatis/*Mapper.xml"></property>
		<!--<property name="configLocation" value="classpath:properties/Mybatis.xml"></property>-->
	</bean>
	<!-- DAO接口所在包名，Spring会自动查找其下的类 -->
	<bean class="org.mybatis.spring.mapper.MapperScannerConfigurer">
		<!-- dao 接口 -->
		<property name="basePackage" value="com.woostore.common.dao" />
		<property name="sqlSessionFactoryBeanName" value="sqlSessionFactory">
		</property>
	</bean>
	
	<tx:annotation-driven transaction-manager="transactionManager" />
	<!--配置事务管理器 -->
	<bean id="auditTransactionManager"
		  class="org.springframework.jdbc.datasource.DataSourceTransactionManager">
		<property name="dataSource" ref="DataSource"></property>
	</bean>

	<!--配置哪些方法,什么情况下需要回滚 -->
	<tx:advice id="auditServiceAdvice" transaction-manager="auditTransactionManager">
		<tx:attributes>
			<!--当代理的service层中的方法抛出异常的时候才回滚,必须加rollback-for参数 -->
			<tx:method name="*save*" propagation="REQUIRED"
					   rollback-for="Throwable" />
			<tx:method name="*add*" propagation="REQUIRED"
					   rollback-for="Throwable" />
			<tx:method name="*get*" propagation="REQUIRED"
					   rollback-for="Throwable" />
			<tx:method name="*delete*" propagation="REQUIRED"
					   rollback-for="Throwable" />
			<tx:method name="*insert*" propagation="REQUIRED"
					   rollback-for="Throwable" />
			<tx:method name="*update*" propagation="REQUIRED"
					   rollback-for="Throwable" />
		</tx:attributes>
	</tx:advice>
</beans>